import AdminContent from './AdminContent';

export const metadata = {
  title: 'Admin — CSV Import | Brew Guide',
  description: 'Admin panel for bulk importing coffee shops via CSV.',
};

export default function AdminPage() {
  return <AdminContent />;
}
